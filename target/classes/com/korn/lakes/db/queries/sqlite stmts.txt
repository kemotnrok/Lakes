CREATE TABLE Beispieltabelle (
    ID INTEGER PRIMARY KEY AUTOINCREMENT,
    Name TEXT NOT NULL,
    FremdID INTEGER,
    FOREIGN KEY (FremdID) REFERENCES AndereTabelle (ID)
);


CREATE TABLE Beispieltabelle (
    ID INTEGER PRIMARY KEY AUTOINCREMENT,
    Name TEXT NOT NULL,
    Nummer INTEGER CHECK(Nummer >= 0 AND Nummer <= 9999),
    FremdID INTEGER,
    FOREIGN KEY (FremdID) REFERENCES AndereTabelle (ID)
);
